<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Health Connect's client library requires minSdk 26, but the app ships
         minSdk 24 (Android 7.x) for GPS/BLE. Health Connect only works on API 26+
         anyway, so allow the higher-minSdk library rather than dropping 7.x users:
         the plugin loads its client lazily and healthConnectSource.isAvailable()
         returns false on older devices, so HR-from-Health-Connect simply no-ops
         there while the rest of the app (incl. live BLE HR) works unchanged. -->
    <uses-sdk tools:overrideLibrary="androidx.health.connect.client" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Supabase OAuth / magic-link redirect deep link. Must match
                 AUTH_DEEP_LINK in src/supabase.js and the Supabase Auth redirect
                 allow-list. App.jsx completes the PKCE exchange on receipt. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="solutions.camboulive.run" android:host="auth-callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Live GPS run tracking (Phase 2). Background location keeps recording with
         the screen off; it is requested only when a run starts and the watcher is
         active only during a run (never 24/7). -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service that holds the location while recording. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Persistent "recording run" notification (runtime-requested on Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Live heart-rate sensor over Bluetooth LE (chest strap / armband / watch
         broadcasting HR). Scanning is flagged neverForLocation so it never implies
         location access; connect is needed to read the sensor. Used only while a
         run is recording — the GPS foreground service already holds the app then,
         so no extra HR foreground service is declared. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Legacy Bluetooth for API <= 30 (minSdk is 24); on those versions scanning
         also uses the fine-location permission already declared above for GPS. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Health Connect: read continuous heart rate from the user's watch post-run
         (HeartRateSeries), so no on-watch workout is required. Requires a Google Play
         health-data declaration + privacy policy before release. The
         @pianissimoproject/capacitor-health-connect plugin supplies the native
         permission-rationale activity; this declares the read scope and lets the app
         discover the Health Connect package. -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>
</manifest>
